{erl_opts, [debug_info]}.

{profiles,
    [
        {test, [
            {erl_opts, [{d, 'TEST', true}]}
        ]}
    ]
}.

{deps, []}.

{shell, [
  % {config, "config/sys.config"},
    {apps, [db]}
]}.
